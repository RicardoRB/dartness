name: dartness
description: Dartness is a framework for building efficient, scalable dart server-side applications. It provides an easy and quick way to develop modern standalone server.
version: 0.0.0
homepage: https://www.example.com

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  shelf: ^1.1.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.15.0
